{
  "name": "agent-os-ecosystem",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "tsc": "pnpm -r tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "dotenv": "^17.4.2",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "4.1.11"
  },
  "dependencies": {
    "@types/better-sqlite3": "^9.6.0"
  }
}
