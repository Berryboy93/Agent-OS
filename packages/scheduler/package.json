{
  "name": "@agent-os/scheduler",
  "version": "3.0.0",
  "description": "Agent-OS durable job scheduler — BullMQ-compatible queue abstraction",
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agent-os/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
