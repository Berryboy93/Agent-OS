{
  "name": "@agent-os/memory",
  "version": "3.0.0",
  "description": "Agent-OS execution and session memory scopes",
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agent-os/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
