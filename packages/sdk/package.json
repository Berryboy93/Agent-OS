{
  "name": "@agent-os/sdk",
  "version": "3.0.0",
  "description": "Agent-OS SDK — defineAgent, defineTool, definePipeline, AgentContext",
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agent-os/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
