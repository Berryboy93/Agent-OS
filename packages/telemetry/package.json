{
  "name": "@agent-os/telemetry",
  "version": "3.0.0",
  "description": "Agent-OS OpenTelemetry instrumentation — traces, metrics, spans",
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agent-os/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
