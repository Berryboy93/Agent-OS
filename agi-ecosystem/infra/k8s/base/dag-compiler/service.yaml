apiVersion: v1
kind: Service
metadata:
  name: dag-compiler
  namespace: agi-ecosystem
  labels:
    app: dag-compiler
spec:
  selector:
    app: dag-compiler
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  - port: 9090
    targetPort: 9090
    name: metrics
  type: ClusterIP
